using System.Collections;
using System.Collections.Generic;
using Gley.UrbanSystem.Internal;
using UnityEngine;

public class HiyerarchyEditor : MonoBehaviour
{
    // Start is called before the first frame update
    public List<GameObject> TrafficLights;
    public List<GameObject> YayaLights;

    public List<GameObject> Intersections;
    void Start()
    {
      //  TrafficSystemManager.Instance.RefreshReferences();
      //  foreach (var light in TrafficSystemManager.Instance.AllTrafficLights)
      //  {
      //      trafficLights.Add(light);
      //      Debug.Log($"Trafik Iþýðý: {light.name}");
      //      a++;
      //  }
      //  
      //  foreach (var yaya in TrafficSystemManager.Instance.AllYayaLights)
      //  {
      //      pedestrianLights.Add(yaya);
      //      Debug.Log($"Yaya Iþýðý: {yaya.name}, BindingType: {yaya.BindingType}");
      //      b++;
      //  }
      // 
      //
      //  foreach (var kavsak in TrafficSystemManager.Instance.AllIntersections)
      //  {
      //      intersections.Add(kavsak);
      //      c++;
      //  }
       
    }

    
}
